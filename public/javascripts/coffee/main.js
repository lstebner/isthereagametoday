// Generated by CoffeeScript 1.6.3
(function() {
  var Schedule;

  Schedule = (function() {
    function Schedule(container) {
      this.container = $(container);
      this.months = this.container.find('.months');
      this.games = this.container.find('.games');
      this.setup_events();
    }

    Schedule.prototype.setup_events = function() {
      var _this = this;
      return this.months.on('click', 'a', function(e) {
        var $a;
        e.preventDefault();
        $a = $(e.currentTarget);
        return _this.show_month($a.data('month'));
      });
    };

    Schedule.prototype.show_month = function(month) {
      this.months.find('.active').removeClass('active');
      this.months.find('[data-month=' + month + ']').addClass('active');
      this.games.find('.month-container.active').removeClass('active');
      return this.games.find('.month-container[data-month=' + month + ']').addClass('active');
    };

    return Schedule;

  })();

  World.Schedule = Schedule;

  /* --------------------------------------------
       Begin main.coffee
  --------------------------------------------
  */


  $(function() {
    if ($('#schedule').length) {
      return World.schedule = new Schedule("#schedule");
    }
  });

}).call(this);
